package com.ui.tests;

import org.openqa.selenium.chrome.ChromeDriver;

import com.ui.pages.HomePage;
import com.ui.pages.LoginPage;

import org.openqa.selenium.WebDriver;

public class LoginTest2 {

	public static void main(String[] args) {

		WebDriver wd = new ChromeDriver();// launch web browser

		/*
		 * HomePage homepage = new HomePage(wd); LoginPage loginPage =
		 * homepage.goToLoginPage(); loginPage.doLoginWith("wawawi3265@devdigs.com",
		 * "Password");
		 */

		/*
		 * BrowserUtility browserutiliity = new BrowserUtility(wd);
		 * browserutiliity.goToWebsite("http://www.automationpractice.pl/index.php");
		 * browserutiliity.maximizeWindow();
		 * 
		 * By signInLinkLocator = By.xpath("//a[contains(text(),\"Sign in\")]");
		 * browserutiliity.clickOn(signInLinkLocator);
		 * 
		 * By emailTextBoxLocator = By.id("email");
		 * browserutiliity.enterText(emailTextBoxLocator, "wawawi3265@devdigs.com");
		 * 
		 * By passwordTextBoxLocator = By.id("passwd");
		 * browserutiliity.enterText(passwordTextBoxLocator, "passWd123");
		 * 
		 * By submitLoginButtonLocator = By.id("SubmitLogin");
		 * browserutiliity.clickOn(submitLoginButtonLocator);
		 */

	}

}
